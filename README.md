# overmind
A meta-harness composition layer: OMA's deterministic scheduler + Omnigent's sandboxed execution + Ruflo's HNSW memory, with MAST failure-mode gates. Composes upstream orchestrators instead of reimplementing them.

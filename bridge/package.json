{
  "name": "overmind-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Exposes Open Multi-Agent's coordinator to the Overmind Python layer",
  "type": "module",
  "scripts": {
    "start": "tsx planner.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@open-multi-agent/core": "^0.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
